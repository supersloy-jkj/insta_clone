# Core
Django>=4.2,<5.0
Pillow>=10.0.0

# Forms
django-crispy-forms>=2.0
crispy-bootstrap5>=0.7

# Production server
gunicorn>=21.2.0

# Static files in production (serve via Django, no separate nginx needed)
whitenoise>=6.6.0

# Database URL parsing (Railway/Render inject DATABASE_URL automatically)
dj-database-url>=2.1.0

# PostgreSQL driver (used in production; falls back to sqlite3 locally)
psycopg2-binary>=2.9.9

# Environment variable management
python-decouple>=3.8
